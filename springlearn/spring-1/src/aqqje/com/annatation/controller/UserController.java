/** * Copyright (c), 2018-2018, XXX有限公司 * FileName: UserController * Author:   Administrator * Date:     2018/5/716:51 * Description: * History: * <author>    <time>    <version>    <desc> * 作者姓名     修改时间      版本号       描述 */package aqqje.com.annatation.controller;import aqqje.com.annatation.service.UserService;import org.springframework.beans.factory.annotation.Autowired;import org.springframework.beans.factory.annotation.Qualifier;import org.springframework.stereotype.Controller;/** * @Autowired （1）中的 required 属性：1.true 则必须注入，无则异常 2. 则有则注入，无则跳过 *            （2）若有一个接口有多个实现 bean 同使用， 则使用时必须指明 bean 的名字且与被使用 bean 的属性名一致 如：@Repository("userRepository") *            （3）可以与 @Qualifier 一起使用，其 @Qualifier 中的值指向 bean 的名字 *            （4）应用在数组类型的属性上, 此时 Spring 将会把所有匹配的 Bean 进行自动装配. *            （5）应用在集合属性上, 此时 Spring 读取该集合的类型信息, 然后自动装配所有与之兼容的 Bean. *            （6）用在 java.util.Map 上时, 若该 Map 的键值为 String, 那么 Spring 将自动装配与之 Map 值类型兼容的 Bean, 此时 Bean 的名称作为键值 * *  @Resource 注解要求提供一个 Bean 名称的属性，若该属性为空，则自动采用标注处的变量或方法名作为 Bean 的名称 * *  @Inject 和 @Autowired 注解一样也是按类型匹配注入的 Bean， 但没有 reqired 属性 * *  建议使用 @Autowired 注解 * * */@Controllerpublic class UserController {    @Autowired    @Qualifier    private UserService userService;    public void addUser(){        System.out.println("UserController: addUser");        userService.addUser();    }}